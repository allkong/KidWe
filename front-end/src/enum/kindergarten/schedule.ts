export enum TeacherScheduleOption {
  '행사' = 'EVENT',
  '수업' = 'CLASS',
}

export const teacherScheduleOptionKeys = Object.keys(TeacherScheduleOption);
export const teacherScheduleOptionValues = Object.values(TeacherScheduleOption);

export enum DirectorScheduleOption {
  '유치원' = 'ALLNOTICE',
}

export const directorScheduleOptionKeys = Object.keys(DirectorScheduleOption);
export const directorScheduleOptionValues = Object.values(
  DirectorScheduleOption
);
