import Icon from '@/assets/icons/notification-fill.svg';

const NotificationButton = () => {
  return (
    <button>
      <img src={Icon} alt="icon" />
    </button>
  );
};

export default NotificationButton;
